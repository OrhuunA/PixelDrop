# Bu dosyanin tek amaci: pytest'in "prepend" import modunda repo kokunu
# sys.path'e eklemesini garantilemek, ki `import pixeldrop` calisma
# dizininden (calistirma yontemi `pytest` mi yoksa `python -m pytest` mi
# oldugundan) bagimsiz her zaman calissin.
